# Movies pillar — attempt log (get ONE entry to standard, then 10 in a row)

Persistence discipline (owner 2026-07-07): if an approach fails, do NOT retry the same approach —
diagnose, log here (attempt #, approach, why it failed), then try a **materially different** approach.
Completion = a fresh detector run (`node _mv_audit.js`) returns zero failures for the target set.

| # | Approach | Result / why it failed | Next different approach |
|---|----------|------------------------|-------------------------|
| 1 | Full-pillar orchestrator run, per-entry opaque | 5 concurrent runs spawned from interrupts → duplicate emails + index races | Kill all; single run only |
| 2 | Single-entry harness `ONE_ID`, product images = external hotlink pick then Fable gate re-fetches | ~2× work: 10 hotlinks fetched (~31s ea) then all discarded (HOTLINKED) + re-fetched → ~8 min/entry | Self-host product images in ONE pass (`SELF_HOST_PRODUCT_IMAGES=1`) |
| 3 | Self-host each product inline via `ensureAlternateSectionImage(forceNew)`, sequential, 15s floor | Faster start, but **hung on product #8** ~2.5 min — provider fetch has no timeout, blocks whole entry | Add hard per-image timeout + allow instant topical reuse (drop forceNew); on timeout fall through |
| 4 | Per-image `withTimeout(60s)` in product-fill + drop forceNew + detector `_mv_audit.js` (self-test passing) | Product-fill no longer freezes (products #8/#9/#10 timed out→fell back to hotlink). Full audit: 50/50 fail — C2_gold 46, C6_formatv 34, C3_grade13 34, C4_imgrender 24. BUT Fable gate repair (`repairBrokenQaImages`) has NO timeout → **hung ~5 min repairing "Terminator 2" slot** (live provider) | Attempt 5: timeout the gate repair + guaranteed instant self-hosted library/pool fallback (hotlinks never pass gate) |
| 5 | Gate: `withTimeout` around repair calls + final `guaranteedLibraryFill` (library/pool, no live provider) | **mv0001 PASS** grade=15/13 gold=true img=true(10) in 399s. Verified: fresh `_mv_audit.js mv0001` → 0 fail; `_mv_grade_check.js mv0001` → 10/10 grade-stamped + 10/10 render-OK (incl. reused pool-mv-010 & mv0050-0). BUT wasted ~3 min: gate did 2×90s live rounds on the "Solaris" slot before instant fallback | Attempt 6: ONE-STRIKE — first timeout goes straight to library; no 2nd live round |
| 6 | ONE-STRIKE + title-keyed library + C14–C17 audit criteria | Backfill: **50/50 mv entries fail** under expanded gate (expected — prior "golden" was false positive). Root causes fixed: aquarium/SaaS boilerplate on movies, content-blind library fill, artistic DDG queries, CRO widget regex, weave markers | Rebuild from mv0001 with new pipeline; done = clean `node _mv_audit.js` |
| 7 | Structural C15b (duplicate-filler co-occurrence) + all-or-nothing `sectionBody` + C22 img-src + render gate + CRO insert before ##4 | **Detector works:** mv0002 pre-fix fails C4/C14/C15/C15b/C17 (all 10 ranks duplicate filler). **Generator fix landed:** `sectionBody` no longer appends stub block to rich prose. **Renderer fix landed:** CRO card now inserts before `## 4` (between entries), not after #3 product-card. Render gate PASS on mv0001+mv0002 (C17a/C17b/C17c/C18). Rebuild mv0002 in progress (timeouts→no-library-match→retry pattern, ~45s/slot). | mv0002 rebuild → audit → mv0001 rebuild (Sci-Fi = actual screenshot page) → full backfill |
