# Golden Template Assumptions Log

> Auto-appended by Cursor agents per `CURSOR_GOLDEN_TEMPLATES.md` §13. One row per assumption applied. Never ask the user — apply the DEFAULTS table (§12 of that file) or the most conservative reading of the canonical specs (`GOLDEN_TEMPLATE_TOP10.md`, `GOLDEN_TEMPLATE_QA.md`), log it here, and keep going.

Log entries are **append-only** — never edit or delete a prior row. If a later session determines an earlier assumption was wrong, log a **new** row noting the correction; do not rewrite history.

| Timestamp | Assumption | Default applied | Entry ID |
|-----------|------------|------------------|----------|
| 2026-07-06T20:23:00-04:00 | `validate-golden.mjs` referenced by `CURSOR_GOLDEN_TEMPLATES.md` task does not exist anywhere in the repo | DEFAULTS: "`validate-golden.mjs` absent" → fall back to `entryScrubPipeline` + gold audit (`auditTop10GoldTemplate`/`auditQaGoldTemplate`) + `rubricSignOff()` as the de facto validator; did not block or fabricate the script | — |
| 2026-07-06T20:30:00-04:00 | CRO card widget: a full-featured hanging card (cord+peg, swing, dismiss, Calendly CTA, CRO Syndicate branding) already exists **live**, wired into `netlify/functions/pulse-machine-entry.js` (`croAdCard()`/`croMobileCard()`/`insertCroAdDesktop()`), but its palette (`#a71c1c`/`#8b0202`, no gold), swing timing (`±1.5deg`/5.5s), and dismiss key (`sessionStorage.croX`/`croMobX`) do not match the owner's dictated spec (crimson `#B91C3F`/oxblood `#7A1128`/gold `#FFB81C`, `±3deg`/4s, `localStorage.croCardDismissed`) | Treated as "incomplete vs spec" per task step 3 → built new spec-exact standalone files `components/cro-card.html` + `css/cro-card.css` rather than editing the working live renderer code | — |
| 2026-07-06T20:30:00-04:00 | Whether to wire `components/cro-card.html` into `pulse-machine-entry.js` | Did **not** wire it — task step 5 caveat says "if renderer already has inline CRO, document path instead of duplicating"; wiring a second CRO card injection risks violating the existing "one CRO card per page" rule in `CURSOR_GOLDEN_TEMPLATES.md` §9. Documented the live path in §9.1/§14 instead | — |
| 2026-07-06T20:30:00-04:00 | Calendly URL to use in the new component | Reused the repo's live Calendly URL `https://calendly.com/korywhiterevops` (found via repo-wide search — used in `pulse-machine-entry.js`, both golden-template MD specs, and multiple pillar pages); did not fall back to the `calendly.com/PLACEHOLDER` default since a repo URL exists | — |
| 2026-07-06T20:30:00-04:00 | Dismiss key mismatch between the new component (`localStorage.croCardDismissed`, per dictated spec) and the live renderer (`sessionStorage.croX`/`croMobX`) | Implemented the dictated `localStorage.croCardDismissed` key exactly as specified in the new standalone `components/cro-card.html` only; left the live renderer's dismiss mechanism untouched per `CURSOR_GOLDEN_TEMPLATES.md` §9 ("Dismiss — Unchanged... never alter without owner passcode 4444") | — |
| 2026-07-06T20:30:00-04:00 | Mobile inline-friendly CSS class name for the new standalone component (not dictated by the spec) | Chose `.cro-card-inline` in the new `cro-card-*` class namespace (fully separate from the live renderer's `cro-*`/`cro-mob-card` namespace) to avoid any collision if both implementations are ever present on the same page during a future migration | — |
| 2026-07-06T20:40:00-04:00 | Garbled owner dictation for B2 CRO item: `"CRO card per §C3 (mobile insertion point: r>euo/ sanr5rSr Li(te ye,n ueahaetscs=u"+cscmwteESeoYc s–nteln"` | Interpreted from prior Q&A spec + §6 gate row **18** (user cited row 17; row 17 is Image sourcing — CRO is row **18**): **"CRO card per §C3 (mobile insertion point: after last depth-section H2, before Related questions)"** — applied to B2 item **9**, expanded §C3 to document both templates (Top 10 mobile: after item #5; Q&A mobile: after last depth-section H2 before Related questions; desktop both: fixed-right renderer-injected); synced `GOLDEN_TEMPLATE_QA.md` §2 | — |
| 2026-07-06T20:35:00-04:00 | Owner CRO mobile placement confirmation arrived as garbled text: `do0Bh r:te .Qenuo iatn i-wevipuneorlqtHO R` | Interpreted as owner confirmation of **Q&A Template B mobile CRO insertion point: before Related questions section**; updated `CURSOR_GOLDEN_TEMPLATES.md` B2 item 9 + new §C3 + DEFAULTS #14; aligned `GOLDEN_TEMPLATE_QA.md` §0.1 mobile bullet | — |
| 2026-07-06T20:36:00-04:00 | Raw owner dictation (unparseable): `1nr(ieoi—a,oe gMlrCieue;he @aid7.odxish ioS ird1 -:panapinexFt: pm.m0,1mHm. r ,r.gmd1 iudl:k phiais gN-oa:a=adiacNltbofr kdronloapiep 5a4 tDSesleh.ePanDontbk` | **action=SKIPPED** — unparseable; no `CURSOR_GOLDEN_TEMPLATES.md` spec change; await clean re-dictation for B2 items 10–16 | — |
| 2026-07-06T20:36:00-04:00 | Garbled owner dictation (multiple times) — latest message unparseable | DEFAULTS §27: garbled user input skipped; completed B2 tail items **10–16** from `GOLDEN_TEMPLATE_QA.md` §2 (Related questions, Sources/YMYL disclaimer, 2–3 in-context internal links, word floor ≥600 / never pad); synced `GOLDEN_TEMPLATE_QA.md` §2 items **13–16** to match | — |
| 2026-07-06T20:37:00-04:00 | Garbled owner input (unparseable) — likely intent B3 Schema for Template B after B2 | Interpreted as **B3. Schema — one JSON-LD** per `GOLDEN_TEMPLATE_QA.md` §3 + §6 gate row **8**: one `@graph` with **QAPage** + **TechArticle** + **BreadcrumbList** (+ optional **FAQPage** when FAQ ≥5); `acceptedAnswer.text` must match rendered `<p class="direct-answer">` / `direct-answer-box` exactly; TechArticle `headline` + `datePublished` + `dateModified` + E-E-A-T (`author` Org Pulse, `editor` Person Kory White); FAQ rich-results note retained; synced `GOLDEN_TEMPLATE_QA.md` §3 `acceptedAnswer.text` row | — |
| 2026-07-06T20:37:00-04:00 | Raw owner dictation (unparseable): `ei>se\"torutr'b} reonor em=rel etsht i l CEd. asnaRdntn etinb ytLs)Dp6pt xi:t2,ia 1xre0cgei et w71xd ern ln an` | **action=SKIPPED** — unparseable; no spec edit with guesses | — |
| 2026-07-06T20:37:00-04:00 | Raw owner dictation (unparseable): `r s rp)gae ae1 xtm eCmrlgdmtpa{na,prd}p p ogetu:Lmeo nsmnlieso tnei` | **action=SKIPPED** — unparseable; no `CURSOR_GOLDEN_TEMPLATES.md` spec change; await clean typed re-dictation | — |
| 2026-07-06T20:50:00-04:00 | Owner dictation for **A3. Schema — one JSON-LD** included `_cg_sprint300_run.js` inline with Article fields | `_cg_sprint300_run.js` **exists** (batch publish runner `cg0518`–`cg0817`) — retained as batch-context cross-ref, **not** a schema builder; Article node = `headline`, `author` Person **Kory White**, `datePublished`, `dateModified`; ItemList = `numberOfItems` **N**, descending order, ListItem 1..N with `name` + `{canonical}#item-{pos}` | — |
| 2026-07-06T20:54:00-04:00 | Garbled owner dictation: `Sources: YMYL topics (health/medicine, money, legal, insuranc...` (truncated) | User cited **B2 item 10** but item **10** is Related questions — interpreted as **B2 item 13 (`## Sources`)** per `GOLDEN_TEMPLATE_QA.md` §2: YMYL topics **health/medicine, money, legal, insurance** → ≥2 authoritative citations + disclaimer from DEFAULTS **YMYL disclaimer** row; non-YMYL → cite statistical/factual claims; synced B2 item **13**, §6 row **9**, DEFAULTS **YMYL unclear** | — |
| 2026-07-06T20:55:00-04:00 | Raw owner dictation (unparseable): `r:li(, + er ie1a m)e` | **action=SKIPPED** — unparseable; no `CURSOR_GOLDEN_TEMPLATES.md` or `GOLDEN_TEMPLATE_QA.md` spec change; await clean re-dictation | — |
| 2026-07-06T20:56:00-04:00 | Raw owner dictation (unparseable): `mplonpeir upsybd}p glonl-mUeao ctmnhl mofxen` | **action=SKIPPED** — unparseable; no spec edit; B2 walkthrough items **11–12** still await clean typed re-dictation (internal links + word floor) | — |
| 2026-07-07T15:20:00-04:00 | `_movies_alt_orchestrator.js` header carries owner note (2026-07-06) "DO NOT attempt golden-template Q&A fixes — face cards only" for the movies (`mv`) pillar, but owner (Kory) issued a newer explicit request today to run golden-template fixes on the movies pillar and email the question ID after each answer is submitted/fixed | Newer explicit owner instruction supersedes the prior header note. Movies golden-template fixes are now the DEFAULT run path (`SKIP_QA_FIXES` off; `NO_FACE_CARDS=1`). All 50 mv entries classify `top10`; 46 need fixes, 4 already compliant. Per-entry email via existing `emailFix()` (question ID + URL). DeepSeek `DAILY_CAP` gate left intact. | mv0001–mv0050 |
| 2026-07-07T15:34:00-04:00 | Owner: "follow Fable's code to ensure all images render before publishing on both mobile and desktop." Movies entries currently carry external retailer HOTLINKS as product images (fail `_FABLE_MASTER_SPEC.md` self-host law + won't reliably render) | Added `_fable_image_render_gate.js` (auditImage + verifyQaAssetRenders live-render guard) wired into `fixOneEntry` (both branches). Publish/`pass` now requires `img.pass` (every cover+section+@@PRODUCT image self-hosted, decodable raster, grade-stamped). Mobile+desktop parity is guaranteed by renderer `entryImgAttrs()` (responsive width:100%/object-fit + onerror + @media). Per-question email now shows image-render status. | mv0001–mv0050 |
| 2026-07-07T15:35:00-04:00 | Owner: "stagger pollinator and ddg images for internal use." `_ddg_facecard_lib.js` had `POLLINATOR_IMAGES_ONLY=true` / `DDG_IMAGES_BANNED=true` (owner 2026-07-05) which forces flux-only and prevents any DDG↔Pollinator alternation | Made both consts env-overridable via `STAGGER_DDG_POLLINATOR`; orchestrator sets it to `1` for the movies project (reversible — unset restores the 2026-07-05 ban). Internal (section/product) image fills now alternate DDG↔Pollinator via existing `_image_provider_alternate.js` (`ensureAlternateSectionImage`, 15s adaptive floor), self-hosted + graded. Covers unaffected (run uses `NO_FACE_CARDS=1`). | mv0001–mv0050 |
| 2026-07-14T01:22:00Z | Owner asked for Square Desk with no pillar picker / fewer buttons; Format Fixer daily-driver URL had too many controls | Applied simplified Square Desk on `/format-fixer` + `/face-card-top-image-generator`: waiting square → title → keyword search → click saves face+top → answer page. Old Format Fixer UI moved to `/format-fixer-full`. Keyword pick uses DDG (owner asked search+click), not Pollinator flux review. Demo `aq9999` when Blobs empty. | square-desk |

## 2026-07-14 — MEG private brief
- Not a Pulse Q&A publish. Personal catch-up notes only (`content/notes/MEG_PRIVATE_BRIEF.md`).
- No golden-template classification / 13/13 path.
- Images: local stock placeholders under `assets/notes/meg/` (not Pollinations / not `/assets/qa` face-card pipeline).

## 2026-07-14T02:24Z — Square fake preview + Cursor manual bake
- Assumption: Desk shows FAKE mosaic only (raw photo + CSS gold title). Click stages; does not bake.
- Assumption: Save queues `_square_apply_queue.json`; Cursor/`_square_manual_apply.js --watch` bakes for real (blank plate + photo + new gold title). Old titled file backed up once as `<id>.oldtitle.jpg`.
- Assumption: Owner picks photos; agent manually applies after Save (past working path per owner/logs).
- 2026-07-14T03:06Z — Updated CLAUDE.md TITLE/FACE-CARD LAW to current Square Builder bake (delete old JPG → new goldTitle). Format Fixer never bakes title. Homepage .sq.jpg = CSS title; answer .jpg = baked title. Checkpoint commit for stuck-thread flush.
