# PILLAR {NAME} FACE/TOPS

**Canonical name pattern:** `PILLAR {NAME} FACE/TOPS`  
**`X` = the pillar name** (not a literal letter). Examples:
- **`PILLAR KPI FACE/TOPS`** · Industry KPIs (`ik`)
- **`PILLAR GTM FACE/TOPS`** · GTM Playbooks (`gp`)
- **`PILLAR CRO FACE/TOPS`** · Pulse Tools (`tl`)

**Status: LOCKED** — change only with owner passcode **`4444`**.  
**Proven:** `PILLAR KPI FACE/TOPS` 2026-07-12 — spot **20/20** face+top PASS.

## What it means (full loop)

1. **Pull up that named pillar** (e.g. KPI).
2. **Pexels pull** — same count as pillar Q&As (known criteria) **plus ~300 bank** for future.
3. Owner **approves** — each keep usable as **face and/or top internal** on **that** pillar.
4. **Apply manually** to every entry: face URL → top **same URL** → next.
5. Review → pre-draft → publish → next pillar.

## Run (KPI example)

```bash
node _manual_pillar_face_tops.js ik
node _manual_pillar_face_tops_spot.js ik 20
```

## Law

`.cursor/rules/manual-image-pass-law.mdc` · `MANUAL_IMAGE_LAW.md` · bypass **`4444`** only
