# OWNER Top-10 URL FORMAT SPEC (saved 2026-07-08 — NOT yet applied; keep current gold template for now)

Owner-requested Top-10 content order (apply later on owner go-ahead; needs golden-template + audit change, passcode 4444):

1. **Title** (H1)
2. **Image** (top image = face-card cover / DDG hero) — NOTE: conflicts with current locked "no top hero on ranking lists" law; owner wants the face-card cover shown as the top image.
3. **Direct Answer**
4. **How We Ranked The Top 10**
5. **1. Title, Image, details** → **2.** … same all the way **past 10** (each rank: heading + its own image + details)
6. **Closing statements** (after item 10)
7. **Mermaid**
8. **FAQ**
9. **Related on PULSE** (currently only in Q&A template — add to Top-10)
10. **Sources**
11. **People Also Searched For** (currently only an optional line in Sources — promote to its own section)
12. etc.

Deltas vs current locked Top-10 template (`_ranking_top10_gold_template.js` TOP10_SECTION_ORDER):
- ADD top image after title (override no-hero law).
- ADD "Related on PULSE" section.
- PROMOTE "People Also Searched For" to its own section.
- Order tail: …rank10 → closing/Bottom Line → mermaid → FAQ → Related on PULSE → Sources → People Also Searched For.

Current enforced order (unchanged for now): Direct Answer → How We Ranked → 1–10 (@@PRODUCT img each) → How to Choose (mermaid) → What to Look For → FAQ → Bottom Line → Sources.

Decision (owner 7-08): KEEP current gold template running; save this for later.
