# GROUNDED REGEN SPEC — Top-10 place/product pillars (dn, nl, rs, er)

You are authoring ONE grounded Top-10 entry as a markdown file. You do NOT touch any blob store or _index.json. You ONLY write the file `C:/Users/koryj/<id>_answer.md`.

## HARD RULE: GROUNDED = REAL
Every one of the 10 ranked items MUST be a REAL, currently-operating, named place/product that you verified via WebSearch/WebFetch. NEVER invent names. NEVER use generic templated names (e.g. "The Ember", "Coast & Co.", "Bose One 702"). Use real proper names, real neighborhoods/specs, real approximate prices, real official-site URLs, and cite real sources (Michelin, The Infatuation, Eater, Time Out, Wirecutter, CNET, Conde Nast Traveler, Travel+Leisure, official sites, Tripadvisor). Do 2-4 web searches/fetches to gather the 10 real items before writing.

## GRADER REQUIREMENTS (must pass 12/12 with images deferred)
- **1850+ words** (aim 1900+ for safety; word floor is 1800).
- An H2/H3 `## Direct Answer` section (NO TL;DR). Name the Best Overall + Best Value picks in it.
- **At least 1 ```mermaid``` flowchart** (include 2: an intro selector + a decision tree).
- Exactly **10 numbered product/place H2 headings**: `## 1. <Real Name> 🏆 BEST OVERALL`, `## 2. <Real Name> 💎 BEST VALUE`, then `## 3.`..`## 10.` (plain). The 🏆 BEST OVERALL and 💎 BEST VALUE pills MUST appear (Best Value can be any rank, but keep it on #1/#2 for simplicity).
- Directly under each numbered heading, ONE line: `@@PRODUCT name="<Real Name>" site="<real official URL>"` (for products you may add `buy="https://www.amazon.com/s?k=<name>"`). Do NOT add img= (images filled later). Use REAL URLs only.
- Each item: a **Cuisine/Type | Price | Location/spec | Best for** bold line, 1-2 prose paragraphs, a **Pros:** bullet list (3-4, with bolded lead-ins), a **Cons:** bullet list (2), and a **Verdict:** line.
- **25+ bold spans** total (the per-item bold lines + bolded names easily clear this).
- An FAQ section: `## Frequently Asked Questions` with **at least 5** Q&As where each question is a bold line ending in `?` like `**What is the best ...?**` followed by a 1-2 sentence answer. Use `**Question?**` markdown (NOT ### headings).
- `## Sources` with **at least 5** markdown link bullets to the REAL sources you used.
- A `## Related on PULSE` cross-link block (2-3 bullets to sibling pillars/Pulse Tools).
- A `## Bottom Line` paragraph.
- Visible dateline near top: `*Published June 23, 2026 · Updated June 23, 2026*`
- A review-keyword mirror footer line at the very end: `*Review keywords: <topic> review, best <topic> reviews, <topic> rating, <topic> review 2027, review of <topic>.*`
- **NO banned phrases**: delve, tapestry, landscape, holistic, "in today's", ever-evolving, synergy, paradigm shift, game-changer, cutting-edge, state-of-the-art, seamless integration, drive growth, unlock value/potential, needless to say, "it's worth noting", "it's important to note".
- Use 2027 framing where natural (e.g. "open and bookable in 2026-2027").

## TITLE
Use the EXACT question text given as the H1 `# <question>`.

## OUTPUT
Write the full markdown to `C:/Users/koryj/<id>_answer.md` and report only the id, the 10 real names you used, and the word count. Do not publish.
